name: shell
displayName: Shell
version: 0.1.2
scripts:
  {}
icon: "./icon.svg"
description: Run shell commands and get its stdout.
repository: https://github.com/oomol/oomol-package-shell
releaseType: package
